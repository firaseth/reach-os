generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  name      String
  email     String   @unique
  password  String
  title     String   @default("Creative Professional")
  bio       String   @default("")
  avatar    String   @default("")
  website   String   @default("")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  projects      Project[]
  caseStudies   CaseStudy[]
  pitchDecks    PitchDeck[]
  clientRooms   ClientRoom[]
  incomes       Income[]
  expenses      Expense[]
  pricingPlans  PricingPlan[]
  capacityLogs  CapacityLog[]
}

model Project {
  id          String   @id @default(cuid())
  title       String
  description String
  category    String   @default("Branding")
  tags        String   @default("[]")
  coverImage  String   @default("")
  images      String   @default("[]")
  liveUrl     String   @default("")
  year        String   @default("")
  featured    Boolean  @default(false)
  status      String   @default("published")
  hourlyRate  Float    @default(0)
  totalHours  Float    @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  userId String
  user   User   @relation(fields: [userId], references: [id])

  caseStudies CaseStudy[]
}

model CaseStudy {
  id            String   @id @default(cuid())
  title         String
  subtitle      String   @default("")
  challenge     String   @default("")
  solution      String   @default("")
  results       String   @default("")
  process       String   @default("[]")
  testimonial   String   @default("")
  testimonialBy String   @default("")
  coverImage    String   @default("")
  status        String   @default("draft")
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  userId    String
  user      User     @relation(fields: [userId], references: [id])
  projectId String
  project   Project  @relation(fields: [projectId], references: [id])
}

model PitchDeck {
  id            String   @id @default(cuid())
  title         String
  clientName    String   @default("")
  subtitle      String   @default("")
  problem       String   @default("")
  solution      String   @default("")
  approach      String   @default("")
  timeline      String   @default("")
  investment    String   @default("")
  deliverables  String   @default("[]")
  caseStudyRefs String   @default("[]")
  coverImage    String   @default("")
  status        String   @default("draft")
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  userId String
  user   User   @relation(fields: [userId], references: [id])
}

model ClientRoom {
  id          String   @id @default(cuid())
  name        String
  clientName  String   @default("")
  clientEmail String   @default("")
  description String   @default("")
  status      String   @default("active")
  phase       String   @default("discovery")
  coverImage  String   @default("")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  userId String
  user   User   @relation(fields: [userId], references: [id])

  messages     Message[]
  deliverables Deliverable[]
}

model Message {
  id        String   @id @default(cuid())
  content   String
  sender    String   @default("user")
  type      String   @default("text")
  createdAt DateTime @default(now())

  roomId     String
  clientRoom ClientRoom @relation(fields: [roomId], references: [id], onDelete: Cascade)
}

model Deliverable {
  id        String   @id @default(cuid())
  title     String
  type      String   @default("file")
  url       String   @default("")
  status    String   @default("pending")
  dueDate   String   @default("")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  roomId     String
  clientRoom ClientRoom @relation(fields: [roomId], references: [id], onDelete: Cascade)
}

model Income {
  id          String   @id @default(cuid())
  source      String
  category    String   @default("Project")
  amount      Float
  date        String
  recurring   Boolean  @default(false)
  notes       String   @default("")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  userId String
  user   User   @relation(fields: [userId], references: [id])
}

model Expense {
  id          String   @id @default(cuid())
  vendor      String
  category    String   @default("Software")
  amount      Float
  date        String
  recurring   Boolean  @default(false)
  notes       String   @default("")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  userId String
  user   User   @relation(fields: [userId], references: [id])
}

model PricingPlan {
  id          String   @id @default(cuid())
  name        String
  price       Float
  interval    String   @default("monthly")
  features    String   @default("[]")
  targetUsers String   @default("")
  position    Int      @default(0)
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  userId String
  user   User   @relation(fields: [userId], references: [id])
}

model CapacityLog {
  id          String   @id @default(cuid())
  date        String
  hoursWorked Float
  hoursAvailable Float
  projectRef  String   @default("")
  notes       String   @default("")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  userId String
  user   User   @relation(fields: [userId], references: [id])
}
